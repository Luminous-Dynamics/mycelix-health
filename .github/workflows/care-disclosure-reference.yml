name: Care Disclosure Reference

on:
  pull_request:
    branches: [main]
    paths:
      - 'crates/care-disclosure-core/**'
      - '.github/workflows/care-disclosure-reference.yml'
  push:
    branches:
      - 'feat/care-disclosure-core-v1'
    paths:
      - 'crates/care-disclosure-core/**'
      - '.github/workflows/care-disclosure-reference.yml'

permissions:
  contents: read

jobs:
  qualify-reference-semantics:
    name: Qualify disclosure semantics
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: crates/care-disclosure-core
    steps:
      - name: Check out exact subject
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Install exact Rust toolchain
        run: |
          rustup toolchain install 1.96.0 --profile minimal --component rustfmt,clippy
          rustc +1.96.0 -Vv
          cargo +1.96.0 -V

      - name: Freeze preflight identities
        run: |
          git rev-parse HEAD | tee /tmp/subject.sha
          sha256sum Cargo.toml Cargo.lock src/lib.rs README.md | tee /tmp/reference.before.sha256
          cargo +1.96.0 metadata --locked --no-deps --format-version 1 > /tmp/reference.metadata.json
          test "$(jq -r '.packages | length' /tmp/reference.metadata.json)" = "1"
          test "$(jq -r '.packages[0].name' /tmp/reference.metadata.json)" = "mycelix-care-disclosure-core"

      - name: Check formatting
        run: cargo +1.96.0 fmt -- --check

      - name: Run tests
        run: cargo +1.96.0 test --locked

      - name: Run Clippy
        run: cargo +1.96.0 clippy --locked --all-targets -- -D warnings

      - name: Verify postflight immutability
        run: |
          test "$(git rev-parse HEAD)" = "$(cat /tmp/subject.sha)"
          sha256sum Cargo.toml Cargo.lock src/lib.rs README.md > /tmp/reference.after.sha256
          diff -u /tmp/reference.before.sha256 /tmp/reference.after.sha256
          git diff --exit-code -- Cargo.toml Cargo.lock src/lib.rs README.md

      - name: Evidence summary
        if: always()
        run: |
          {
            echo '## Care disclosure reference evidence'
            echo
            echo "- subject: $(git rev-parse HEAD)"
            echo "- runner: ubuntu-24.04"
            echo "- rustc: $(rustc +1.96.0 --version)"
            echo "- cargo: $(cargo +1.96.0 --version)"
            echo '- scope: disclosure/task representation and attenuation semantics only'
            echo '- non-claim: no encryption/Holochain/clinical/legal/AI-safety qualification'
          } >> "$GITHUB_STEP_SUMMARY"
