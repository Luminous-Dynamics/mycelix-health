# Integration note

The durable producer composition is implemented in a separate child proof line (QUAL-EVID-009C3). This store remains an independent local-durability theorem.

The child must not reinterpret a green integration run as an independent PASS for this store. Conversely, this store must not expose or depend on the producer implementation.
