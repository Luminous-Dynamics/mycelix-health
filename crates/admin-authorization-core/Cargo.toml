[workspace]

[package]
name = "mycelix-admin-authorization-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
license = "AGPL-3.0-or-later"
description = "Reference semantics for fail-closed admin authorization and explicit bootstrap"
publish = false

[lib]
path = "src/lib.rs"
