[package]
name = "fhir_mapping"
version = "0.1.0"
edition = "2021"
description = "FHIR R4 resource mapping coordinator zome - clinical interoperability"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }
holochain_serialized_bytes = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
getrandom = { workspace = true }
fhir_mapping_integrity = { path = "../integrity" }
mycelix-health-shared = { path = "../../shared" }

[features]
default = []
