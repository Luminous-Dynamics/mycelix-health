[package]
name = "moment_integrity"
version = "0.1.0"
edition = "2021"
description = "Universal Health Moment - global health context awareness for everyone"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdi = { workspace = true }
holochain_serialized_bytes = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[features]
default = []
