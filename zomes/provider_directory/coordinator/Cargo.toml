[package]
name = "provider_directory"
version = "0.1.0"
edition = "2021"
description = "Provider Directory coordinator zome - NPI registry, credentials, specialties"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }
holochain_serialized_bytes = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
getrandom = { workspace = true }
provider_directory_integrity = { path = "../integrity" }
mycelix-health-shared = { path = "../../shared" }

[features]
default = []
