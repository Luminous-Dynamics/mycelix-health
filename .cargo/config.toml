[build]
target = "wasm32-unknown-unknown"

[target.wasm32-unknown-unknown]
rustflags = [
    "-C", "opt-level=z",
    # Enable getrandom custom support via cfg (for getrandom 0.3+)
    "--cfg", "getrandom_backend=\"custom\"",
]
